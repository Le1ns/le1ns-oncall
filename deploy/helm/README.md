# Helm Chart

Reserved for the Kubernetes packaging step.

Planned resources:

- Deployment and Service for `oncall-api`.
- PostgreSQL as an external dependency or optional bundled chart.
- ConfigMap and Secret for integrations.
- Grafana plugin installation strategy.
- Ingress for Alertmanager webhook endpoint.
