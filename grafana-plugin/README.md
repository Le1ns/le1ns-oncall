# OnCall Grafana App

Grafana App Plugin UI for the local OnCall platform.

The plugin is intentionally thin: it renders pages inside Grafana and calls the standalone `oncall-api` backend for schedules, people, alerts, Jira state, notifications, and reports.
